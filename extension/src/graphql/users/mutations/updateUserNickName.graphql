mutation UpdateUserNickName($uid: String!, $nickname: String) {
  updateUserNickName(uid: $uid, nickname: $nickname) {
    errors {
      field
      message
    }
    user {
      id
      name
      nickname
      email
      photoUrl
      watchedMovies
      joinedAt
      updatedAt
    }
  }
}
