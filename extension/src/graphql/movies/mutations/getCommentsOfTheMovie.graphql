mutation getCommentsOfTheMovie(
  $limit: Int!
  $mid: String!
  $page: Int
  $time: String
) {
  getCommentsOfTheMovie(limit: $limit, mid: $mid, page: $page, time: $time) {
    movie {
      mid
      name
    }
    lastPage
    totalCommentCount
    pastLoadedCount
    comments {
      cid
      commentedUserUid
      createdAt
      message
    }
  }
}
