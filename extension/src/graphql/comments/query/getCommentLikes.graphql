query getCommentLikes($cid: String!) {
  getCommentLikes(cid: $cid) {
    likes {
      uid
      name
      nickname
      photoUrl
      followerCount
      followingCount
    }
    likesCount
  }
}
