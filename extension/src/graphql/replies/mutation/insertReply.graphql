mutation insertReply($options: ReplyInput!) {
  insertReply(options: $options) {
    commentedUserId
    createdAt
    message
    movieId
    parentCommentId
    parentReplyId
    platformId
    id
  }
}
