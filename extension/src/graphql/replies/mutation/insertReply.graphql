mutation insertReply($options: ReplyInput!) {
  insertReply(options: $options) {
    commentedUserUid
    createdAt
    message
    movieMid
    parentCommentCid
    parentReplyRid
    platformId
    rid
  }
}
