import React, { useCallback } from 'react';

import { PopupParent } from './popup.styles';

const Popup = () => {
  const SelectedElement = useCallback(() => {}, []);
  return <PopupParent></PopupParent>;
};

export default Popup;
