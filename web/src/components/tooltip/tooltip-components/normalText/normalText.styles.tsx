import styled from "styled-components";

export const StyledText = styled.div`
    font-size: 10px;
    backdrop-filter: contrast(0.5);
    padding: 6px;
    border-radius: 18px;
    font-weight: 600;
`