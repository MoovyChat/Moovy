query GetCommentsOfTheMovie($mid: String!, $page: Int) {
  getCommentsOfTheMovie(mid: $mid, page: $page) {
    id
    movie {
      id
      name
      parentTitleName
    }
    hasMoreComments
    totalCommentCount
    comments {
      ...FullComment
    }
    lastPage
    pastLoadedCount
  }
}
