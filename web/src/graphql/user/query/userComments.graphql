query getCommentsOfTheUser($uid: String!) {
  getCommentsOfTheUser(uid: $uid) {
    id
    user {
      id
      name
      email
      photoUrl
      nickname
      updatedAt
      watchedMovies
      joinedAt
    }
    comments {
      ...FullComment
    }
  }
}
