query getCommentsOfTheUser($uid: String!) {
  getCommentsOfTheUser(uid: $uid) {
    user {
      id
      name
      email
      photoUrl
      nickname
      updatedAt
      watchedMovies
      joinedAt
    }
    comments {
      ...FullComment
    }
  }
}
